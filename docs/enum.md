I guess I have already written somewhere else about this

# Enum

- Enumeration type.
- special kind of scalar type.
- restricted to a particular set of allowed values.

```ts
export enum Unit {
  BYTE,
  KILOBYTE,
  MEGABYTE,
  GIGABYTE,
}
```
